import { useState } from 'react';
import { Card, Typography } from '@mui/material';
import { PieCharts } from '@mui/x-charts';

export default function Headers() {
    return (
        <div
        style={{
            height: "30%",
            width: "80%",
            margin: "auto",
            textAlign:"center",
            backgroundColor: "#2b2d42",
            color: "white"
        }}
    >
        <Typography variant='h3'>Raajas Sode</Typography>

    </div>
    )
}